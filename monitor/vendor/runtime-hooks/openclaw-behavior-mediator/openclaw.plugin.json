{
  "id": "behavior-mediator",
  "name": "Behavior Mediator",
  "description": "Canonical trace-aware JSONL behavior stream for OpenClaw.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "runsDirectory": {
        "type": "string",
        "description": "Absolute path to the root directory that stores one trace run per task."
      },
      "artifactsEnabled": {
        "type": "boolean",
        "description": "Whether to persist full redacted tool input/output sidecars under each run directory."
      },
      "autoDiagnosisEnabled": {
        "type": "boolean",
        "description": "Whether to trigger the CodeTracer diagnosis runner automatically after a run reaches a terminal state."
      },
      "diagnosisScript": {
        "type": "string",
        "description": "Absolute path to the Python script that builds the CodeTracer bundle and runs diagnosis for one run directory."
      },
      "diagnosisPython": {
        "type": "string",
        "description": "Python executable used to spawn the diagnosis runner."
      },
      "outputFile": {
        "type": "string",
        "description": "Deprecated legacy hot-log path. If provided, runsDirectory defaults to <outputFile parent>/runs."
      },
      "capturePreviewChars": {
        "type": "integer",
        "minimum": 128,
        "description": "Maximum characters stored in redacted preview fields."
      },
      "captureNetwork": {
        "type": "boolean",
        "description": "Whether to capture non-LLM fetch evidence as network spans."
      },
      "securityEnabled": {
        "type": "boolean",
        "description": "Whether to run the online Transpect security guards during input, planning, tool, and network phases."
      },
      "securityMode": {
        "type": "string",
        "enum": ["off", "audit", "enforce"],
        "description": "Online guard mode. off disables guard calls, audit records decisions without blocking, enforce blocks high-risk actions."
      },
      "policyPath": {
        "type": "string",
        "description": "Optional absolute path to the local allow/block policy file used by the security bridge."
      },
      "llmJudge": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Whether gray-zone actions may be escalated to an LLM judge."
          },
          "mode": {
            "type": "string",
            "enum": ["gray_zone_only"],
            "description": "When enabled, only deterministic gray-zone actions are sent to the LLM judge."
          }
        }
      },
      "securityPython": {
        "type": "string",
        "description": "Python executable used to invoke the Transpect security bridge."
      },
      "securityBridgeScript": {
        "type": "string",
        "description": "Absolute path to guardrail/agent_defense/bridge.py."
      },
      "traceEval": {
        "type": "boolean",
        "description": "Whether to trace eval and Function constructor activity."
      },
      "redactHeaders": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Case-insensitive HTTP header names that should always be redacted."
      },
      "redactPatterns": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Regular expression patterns used to redact secrets from previews."
      }
    }
  }
}
